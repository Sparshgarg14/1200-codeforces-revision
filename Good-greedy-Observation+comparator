import com.sun.security.jgss.GSSUtil;

import javax.swing.tree.TreeNode;
import java.io.*;
import java.util.*;



//JAI SHREE RAM
public class Codeforces {
    public static int mod = 1000000;

    //The problems are easy and if I m not going anywhere with some solution, come back, think again from basic facts and logic
    //    try to see if the process is good when working in reverse(reverse numbers from 1-n) and think of parity
    //    even first or odd first to print
    //    try to make generalized cases - aaaa...bbbb...a a a b bb a ..a.. at the end what I need to do ultimately
    //    question says to solve a problem given three options , a,b,c , try taking c absent and then think can u do it ?, it makes the problem simpler
    //    u have to take out the min, think like what min to take out that also takes max with it
    //    when logic doesn't strike you on what to perform and array given  , try to think in terms of array index
    //    when they want to have condition as true , try and see what is causing it to be false,(what if all chars>k , whole string is the ans)
    //    chars are between 1-26 only


    //The Problem statement
    /*
    if I have odd=0 then no for sure
    else if odd>=1 , ek odd to mil gya h ab ..
    next part ka sum should be even for sure to get the odd sum...
    when i can say that the sum will be even , if x-1 even ho or (x-1)is even and (x-1)/2 even and (x-1)/2 odd h mere paas


     */


    //Small observations in the question
    /*


     */


    //Algo that can be claimed
    /*


     */


    public static void main(String[] args) throws IOException {
        FastReader fr = new FastReader();
        int t = fr.nextInt();
        while(t-- > 0) {
      int n   = fr.nextInt();
      List<Pair>list1 = new ArrayList<>();
      List<Pair>list2 = new ArrayList<>();
      List<Pair>list3 = new ArrayList<>();
      int n1 = n;

          for (int i = 0; i < n; i++) {
              int x  = fr.nextInt();
              list1.add(new Pair(x, i));
          }
          for (int i = 0; i < n; i++) {
              int x  = fr.nextInt();
              list2.add(new Pair(x, i));
          }
          for (int i = 0; i < n; i++) {
              int x  = fr.nextInt();
              list3.add(new Pair(x, i));
          }


      list1.sort(Comparator.comparingInt((Pair p)-> p.a).thenComparingInt(p -> p.b));
          list2.sort(Comparator.comparingInt((Pair p)-> p.a).thenComparingInt(p -> p.b));
            list3.sort(Comparator.comparingInt((Pair p)-> p.a).thenComparingInt(p -> p.b));
            Collections.reverse(list1);
            Collections.reverse(list2);
            Collections.reverse(list3);
//            System.out.println(list1);
//            System.out.println(list2);
//            System.out.println(list3);

           int ans =-1;
            for (int i = 0; i < 3; i++) {
                for (int j = 0; j < 3; j++) {
                    for (int k = 0; k < 3; k++) {
                        if(list1.get(i).b!=list2.get(j).b && list2.get(j).b!=list3.get(k).b && list1.get(i).b!=list3.get(k).b){
                            ans=  Math.max(ans, list1.get(i).a + list2.get(j).a + list3.get(k).a);
                        }
                    }
                }
            }
            System.out.println(ans);






        }
    }









    public static void Swap(int i , int j ){
        int temp=0;
        i =j;

    }
    // Function to compute GCD of two numbers
    public static int gcd(int a, int b) {
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    }



    //  Binary Exponentiation (modular fast power)
    static long binPow(long a, int b) {
        long res = 1;
        while (b > 0) {
            if ((b & 1) == 1) res = res * a % mod;
            a = a * a % mod;
            b >>= 1;
        }
        return res;
    }

    public static class Pair {
        public int a, b;
        Pair(int a, int b) {
            this.a = a;
            this.b = b;
        }
        @Override
        public String toString() {
            return a + " -> " + b;
        }
        @Override
        public boolean equals(Object o) {
            if (this == o) return true;
            if (o == null || getClass() != o.getClass()) return false;
            Pair that = (Pair) o;
            return a == that.a && b == that.b;
        }
    }
}

class FastReader {
    BufferedReader br;
    StringTokenizer st;

    public FastReader() {
        br = new BufferedReader(new InputStreamReader(System.in));
    }

    String next() {
        while (st == null || !st.hasMoreElements()) {
            try {
                String line = br.readLine();
                if (line == null) return null;
                st = new StringTokenizer(line);
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
        return st.nextToken();
    }

    int nextInt() {
        return Integer.parseInt(next());
    }

    long nextLong() {
        return Long.parseLong(next());
    }

    double nextDouble() {
        return Double.parseDouble(next());
    }

    String nextLine() {
        String str = "";
        try {
            if (st.hasMoreTokens()) str = st.nextToken("\n");
            else str = br.readLine();
        } catch (IOException e) {
            e.printStackTrace();
        }
        return str;
    }
}
