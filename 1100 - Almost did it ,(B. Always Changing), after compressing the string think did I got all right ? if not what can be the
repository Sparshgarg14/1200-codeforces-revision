import com.sun.security.jgss.GSSUtil;

import javax.swing.*;
import java.io.*;
import java.util.*;



//JAI SHREE RAM
public class Codeforces {
    public static int mod = 1000000;

    //The problems are easy and if I m not going anywhere with some solution, come back, think again from basic facts and logic
    //    try to see if the process is good when working in reverse(reverse numbers from 1-n) and think of parity
    //    even first or odd first to print
    //    try to make generalized cases - aaaa...bbbb...a a a b bb a ..a.. at the end what I need to do ultimately
    //    question says to solve a problem given three options , a,b,c , try taking c absent and then think can u do it ?, it makes the problem simpler
    //    u have to take out the min, think like what min to take out that also takes max with it
    //    when logic doesn't strike you on what to perform and array given  , try to think in terms of array index
    //    when they want to have condition as true , try and see what is causing it to be false,(what if all chars>k , whole string is the ans)
    //    chars are between 1-26 only


    //The Problem statement
    /*


     */


    //Small observations in the question
    /*
    if I make the string single length , it is still alternating

     if it contains all chars of same char and if length >=3 , ans =-1
     have to delete alternate chars
     at the end of the day  I want to make the chars alternating 10101010101010101001111100000101010
     11110000
     length is not the invariant , maybe its the number of ones and zeros , we can say that if its alternating
     10101, maybe cnt1=cnt0 or abs(cnt1-cnt0)=1 therefore abs(cnt1-cnt0)<=1, I want to make it
     1110
     011
     2ones and 1 zero
     1 one 1 zero
     when alternating then max length of 0 is 1 , and 1 is 1 too
     001001
     */



    //Algo that can be claimed



    public static void main(String[] args) throws IOException {
        FastReader fr = new FastReader();
        int t = fr.nextInt();
        while (t-- > 0) {
         int n = fr.nextInt();
         String s  = fr.next();
         int zero=0;
         int ones =0;

            for (char ch :s.toCharArray()) {
                   if(ch=='0'){
                    zero++;
                   }
                   else{
                       ones++;
                   }
            }
//            1000011100
//            greedily -> 010001 -> -00110 01010110, 01011011
            if(s.length()==1){
                System.out.println(0);
                continue;
            }
            if(Math.abs(ones - zero)>2){
                System.out.println(-1);
                continue;
            }
            int cnt1 = 0;
            int cnt0 = 0;
            int ans = 0;
// CHANGED: instead of maximum block lengths,
            // count how many 0-blocks and 1-blocks exist
            int length1 = 0;
            int length0 = 0;

            

            for (char ch : s.toCharArray()) {

                if (ch == '0') {

                    // A block of 1s has ended
                    if (cnt1 > 0) {

                        // Your original block contribution
                        ans += Math.max(0, cnt1 - 1);

                        // One '1' survives from this block
                        length1++;

                        cnt1 = 0;
                    }

                    cnt0++;

                } else {

                    // A block of 0s has ended
                    if (cnt0 > 0) {

                        // Your original block contribution
                        ans += Math.max(0, cnt0 - 1);

                        // One '0' survives from this block
                        length0++;

                        cnt0 = 0;
                    }

                    cnt1++;
                }
            }

            // Handle the last block
            if (cnt0 > 0) {
                ans += Math.max(0, cnt0 - 1);
                length0++;
            }

            if (cnt1 > 0) {
                ans += Math.max(0, cnt1 - 1);
                length1++;
            }

            // NEW: compare imbalance of original and compressed string
            int deltaN = zero - ones;
            int deltaL = length0 - length1;

            ans += Math.max(0, Math.abs(deltaN - deltaL) - 1);

            System.out.println(ans);
        }
//            after deleting the chars, I should have thought , are those
//            deletions valid ?
    }


    public static  int ans=0;
        public static int dfs(int node , List<List<Integer>>tree){
        if(tree.get(node).isEmpty()){
            ans++;
            return 1;
        }
        List<Integer>h  =new ArrayList<>();

        for(int child:tree.get(node)){
            h.add(dfs(child,tree));
        }
        if(h.size()==1){
            ans++;
            return h.get(0)+1;
        }
        Collections.sort(h);
        Collections.reverse(h);
        ans+=h.get(1)+1;
        return h.get(0)+1;

    }









    public static void Swap(int i , int j ){
        int temp=0;
        i =j;

    }
    // Function to compute GCD of two numbers
    public static int gcd(int a, int b) {
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return a;
    }



    //  Binary Exponentiation (modular fast power)
    static long binPow(long a, int b) {
        long res = 1;
        while (b > 0) {
            if ((b & 1) == 1) res = res * a % mod;
            a = a * a % mod;
            b >>= 1;
        }
        return res;
    }



    // ============================================================
// TREE TEMPLATE
// ============================================================


//    dfs traversal
//    static int dfs(int node, List<List<Integer>> tree) {
//
//        int ans = 0;
//
//        for (int child : tree.get(node)) {
//            ans += dfs(child, tree);
//        }
//
//        return ans;
//    }

    // 1. Parent Array -> Rooted Tree
    static List<List<Integer>> buildTree(int n, int[] parent) {

        List<List<Integer>> tree = new ArrayList<>();

        for (int i = 0; i <= n; i++) {
            tree.add(new ArrayList<>());
        }

        for (int i = 2; i <= n; i++) {
            tree.get(parent[i]).add(i);
        }

        return tree;
    }

    // 2. Edges -> Undirected Tree
    static List<List<Integer>> buildUndirectedTree(
            int n, int[][] edges) {

        List<List<Integer>> tree = new ArrayList<>();

        for (int i = 0; i <= n; i++) {
            tree.add(new ArrayList<>());
        }

        for (int[] edge : edges) {

            int u = edge[0];
            int v = edge[1];

            tree.get(u).add(v);
            tree.get(v).add(u);
        }

        return tree;
    }

    public static class Pair {
        public int a, b;
        Pair(int a, int b) {
            this.a = a;
            this.b = b;
        }
        @Override
        public String toString() {
            return a + " -> " + b;
        }
        @Override
        public boolean equals(Object o) {
            if (this == o) return true;
            if (o == null || getClass() != o.getClass()) return false;
            Pair that = (Pair) o;
            return a == that.a && b == that.b;
        }
    }
}

class FastReader {
    BufferedReader br;
    StringTokenizer st;

    public FastReader() {
        br = new BufferedReader(new InputStreamReader(System.in));
    }

    String next() {
        while (st == null || !st.hasMoreElements()) {
            try {
                String line = br.readLine();
                if (line == null) return null;
                st = new StringTokenizer(line);
            } catch (IOException e) {
                e.printStackTrace();
            }
        }
        return st.nextToken();
    }

    int nextInt() {
        return Integer.parseInt(next());
    }

    long nextLong() {
        return Long.parseLong(next());
    }

    double nextDouble() {
        return Double.parseDouble(next());
    }

    String nextLine() {
        String str = "";
        try {
            if (st.hasMoreTokens()) str = st.nextToken("\n");
            else str = br.readLine();
        } catch (IOException e) {
            e.printStackTrace();
        }
        return str;
    }
}



